$(function() {
  $('#slides').slidesjs({
    width : 600,
    height : 200,
    play : {
      active : true,
      auto : true, //mudar para true
      interval : 7000,
      swap : true
    }
  });
});